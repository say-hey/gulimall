spring:
  application:
    name: guliamll-gateway
  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:8848
        namespace: gulimall-gateway
#        name: gulimall-gateway
#        file-extension: yml
